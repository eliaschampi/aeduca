import { createColorScheme } from '@lumi-ui/svelte/color-scheme';
import { colorSchemeOptions } from './color-scheme-options';

export { colorSchemeOptions };

export const colorScheme = createColorScheme(colorSchemeOptions);
